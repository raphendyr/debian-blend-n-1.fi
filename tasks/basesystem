Format: https://blends.debian.org/blends/1.1
Task: basesystem
Description: Debian core for Debian Blend by n-1.fi
 This metapackage will install packages useful for every Debian system.
Install: true

Depends:
  base-passwd (>= 3.5.46),
  bash (>= 4.4),
  bsdutils (>= 1:2.33),
  coreutils (>= 8.30),
  dash,
  diffutils,
  dpkg (>= 1.19),
  e2fsprogs,
  findutils (>= 4.6.0),
  findutils (>= 4.6.0),
  grep (>= 3.3),
  gzip (>= 1.9),
  hostname (>= 3.21),
  init,
  man-db,
  ncurses-base (>= 6.1),
  ncurses-bin (>= 6.1),
  sed (>= 4.7),
  util-linux (>= 2.33),
Why: essential packages (roots)

Depends:
  systemd,
  systemd-cron,
  systemd-sysv,
Why systemd

Depends:
  apt,
  apt-listchanges,
  apt-utils,
  debconf,
  dialog,
  locales,
Recommends:
  aptitude,
  popularity-contest,
  reportbug,
  unattended-upgrades,
Why: Debian package management and core stuff

Recommends:
  cryptsetup,
  gdisk,
Why: basic disk management tools

Depends:
  gawk,
  less,
  pv,
  xmlstarlet,
Why: more basic shell utils

Depends:
  bzip2,
  lz4,
  p7zip,
  unar,
Why: compression

Recommends:
  curl,
  openssh-client,
  rsync,
  wget,
Why: network connectivity

Recommends: lm-sensors

Recommends:
  ipset,
  vlan,
Suggest:
  iptables-persistent,
Why: kernel networking

Recommends: burp
Why: the tool for backups

Recommends: passwdqc

Suggests:
  hwinfo,
Why: generic system debugging and info
