Format: https://blends.debian.org/blends/1.1
Task: shell
Description: Debian Blend by n-1.fi - shell environment
 This metapackage will install packages useful when working with shell
 in Debian.
Install: true

Depends: blend-n-1.fi-basesystem

Depends:
  bash (>= 4.4),
  zsh (>= 5.3),
Recommends:
  blend-n-1.fi-config-bash,

Recommends: fzf, fd-find
Why: Enables fuzzy text and file search in the shell and vim

Depends:
  nano,
  vim,
Recommends:
  blend-n-1.fi-config-nano,
Why: Text editors


Recommends: git
Why: A version control system used for development and for shared dotfiles

Recommends: stow
Why: Required for shared dotfiles

Recommends: octave


# Secrets

Recommends:
  pass,
  pass-extension-otp,
  pinentry-tty,
Why: Savely store passwords on disk


# Sys Management

Recommends:
 dfc,
 htop,
 iotop,
Why: system monitoring

Recommends:
  bind9-dnsutils,
  bind9-host,
  ipcalc,
  iperf,
  iputils-ping,
  ipv6calc,
  miniupnpc,
  sipcalc,
  tcpdump,
  telnet,
  traceroute,
Why: network debug tools

Recommends:
  gddrescue,
  testdisk,
Why: recovery tools

Recommends: mtp-tools
