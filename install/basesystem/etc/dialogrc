#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 2

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Show scrollbar in dialog boxes?
use_scrollbar = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = ON

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (GREEN,BLACK,ON)

# Shadow color
shadow_color = (BLACK,BLACK,ON,ON)

# Dialog box color
dialog_color = (WHITE,BLACK,ON)

# Dialog box title color
title_color = screen_color

# Dialog box border color
border_color = (WHITE,BLACK,OFF)

# Active button color (the < and > markers)
button_active_color = (YELLOW,YELLOW,ON)

# Inactive button color
button_inactive_color = (WHITE,BLACK,OFF)
# Active button key color (reverted on focused button)
button_key_active_color = (BLACK,YELLOW,OFF,OFF,ON)

# Inactive button key color
button_key_inactive_color = (YELLOW,BLACK,ON)

# Active button label color
button_label_active_color = (BLACK,YELLOW,OFF)

# Inactive button label color
button_label_inactive_color = (WHITE,BLACK,ON)

# Input box color
inputbox_color = dialog_color

# Input box border color
inputbox_border_color = border_color

# Search box color
searchbox_color = (YELLOW,BLACK,ON)

# Search box title color
searchbox_title_color = title_color

# Search box border color
searchbox_border_color = border_color

# File position indicator color
position_indicator_color = title_color

# Menu box color
menubox_color = dialog_color

# Menu box border color
menubox_border_color = border_color

# Item color
item_color = (WHITE,BLACK,OFF)

# Selected item color
item_selected_color = (WHITE,BLACK,ON)

# Tag color
tag_color = button_inactive_color

# Selected tag color
tag_selected_color = button_active_color

# Tag key color
tag_key_color = item_selected_color

# Selected tag key color
tag_key_selected_color = button_key_inactive_color

# Check box color
check_color = button_label_inactive_color

# Selected check box color
check_selected_color = button_label_active_color

# Up arrow color
uarrow_color = screen_color

# Down arrow color
darrow_color = screen_color

# Item help-text color
itemhelp_color = screen_color

# Active form text color
form_active_text_color = searchbox_color

# Form text color
form_text_color = dialog_color

# Readonly form item color
form_item_readonly_color = button_inactive_color

# Dialog box gauge color
gauge_color = dialog_color

# Dialog box border2 color
border2_color = (BLACK,BLACK,ON)

# Input box border2 color
inputbox_border2_color = border2_color

# Search box border2 color
searchbox_border2_color = border2_color

# Menu box border2 color
menubox_border2_color = border2_color
